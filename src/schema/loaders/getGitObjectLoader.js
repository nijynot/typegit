import DataLoader from 'dataloader';
import mysql from '../../config/mysql.js';
import { Repository } from '../models/Repository.js';

module.exports = () => {
  // return new DataLoader(async (ids) => {
  //   const gitObjectPromises = ids.map((id) => (
  //
  //   ));
  // });
};
